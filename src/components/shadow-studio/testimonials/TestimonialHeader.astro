---

---

<header
  id="testimonials-header"
  aria-labelledby="testimonios-heading"
  class="relative mb-3 lg:mb-24 overflow-hidden rounded-sm bg-primarybg border-t border-b border-white/5 py-20 lg:py-32 opacity-0 transition-opacity duration-700"
>
  <div class="absolute inset-0 pointer-events-none">
    <div
      class="absolute inset-0 bg-gradient-to-b from-primarybg via-tertiary/10 to-primarybg"
    >
    </div>
    <div
      class="absolute inset-0 opacity-[0.03]"
      style="background-image: url('data:image/svg+xml,%3Csvg viewBox=%220 0 200 200%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cfilter id=%22noiseFilter%22%3E%3CfeTurbulence type=%22fractalNoise%22 baseFrequency=%220.65%22 numOctaves=%223%22 stitchTiles=%22stitch%22/%3E%3C/filter%3E%3Crect width=%22100%25%22 height=%22100%25%22 filter=%22url(%23noiseFilter)%22/%3E%3C/svg%3E');"
    >
    </div>

    <div
      class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full flex justify-center items-center opacity-[0.04]"
    >
      <span
        class="text-[18vw] font-black uppercase tracking-tighter text-transparent leading-none outline-text animate-pulse-slow"
      >
        VOICES
      </span>
    </div>
  </div>

  <div class="relative z-10 container mx-auto px-6">
    <div
      class="flex flex-col lg:flex-row lg:items-end justify-between gap-12 lg:gap-20"
    >
      <div class="max-w-3xl flex-1">
        <div
          class="flex items-center gap-4 mb-6 reveal-item"
          style="--delay: 100ms"
        >
          <span class="h-[1px] w-0 bg-aux-secondary animate-expand-line"></span>
          <span
            class="text-aux-secondary text-xs font-bold tracking-[0.3em] uppercase opacity-0 animate-fade-in-right"
          >
            Testimonios Reales
          </span>
        </div>

        <h2
          id="testimonios-heading"
          class="text-5xl lg:text-7xl font-black tracking-tighter text-white leading-[0.95] mb-6 reveal-item"
          style="--delay: 300ms"
        >
          La Confianza se <br />
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r from-secondary via-aux-primary to-secondary bg-[length:200%_auto] animate-shine"
          >
            tatúa en Shadow.
          </span>
        </h2>

        <p
          class="text-lg lg:text-xl text-gray-400 font-light max-w-xl leading-relaxed border-l-2 border-white/10 pl-6 reveal-item"
          style="--delay: 500ms"
        >
          Más que clientes, coleccionistas. Descubre las historias de quienes
          eligieron llevar nuestro arte en su piel para siempre.
        </p>
      </div>

      <div
        class="flex flex-col items-start lg:items-end gap-8 w-full lg:w-auto"
      >
        <div class="reveal-item" style="--delay: 700ms">
          <div
            class="flex lg:items-center gap-5 p-4 rounded-xl bg-white/5 backdrop-blur-md border border-white/10"
          >
            <div class="text-right">
              <div class="flex items-center lg:justify-end gap-1 mb-1">
                {
                  [1, 2, 3, 4, 5].map(() => (
                    <svg
                      class="h-4 w-4 text-aux-secondary fill-current"
                      aria-hidden="true"
                    >
                      <use href="./shadow-studio-Sprite.svg#star" />
                    </svg>
                  ))
                }
              </div>
              <p
                class="text-xs text-gray-400 font-medium tracking-wide uppercase"
              >
                Excelencia Certificada
              </p>
            </div>
            <div class="h-10 w-[1px] bg-white/20"></div>
            <div class="h-full my-auto items-center justify-center text-center">
              <span
                class="block text-2xl md:text-3xl lg:text-4xl font-bold text-white leading-none"
                >4.9</span
              >
            </div>
          </div>
        </div>

        <div class="reveal-item" style="--delay: 900ms">
          <button
            type="button"
            class="group relative inline-flex items-center justify-center gap-3 px-8 py-4 bg-aux-secondary text-primarybg text-sm font-extrabold uppercase tracking-widest overflow-hidden rounded-sm transition-all hover:bg-white hover:text-primarybg hover:shadow-[0_0_20px_rgba(227,160,70,0.6)]"
          >
            <span class="relative z-10">Dejar mi testimonio</span>
            <svg
              class="w-4 h-4 relative z-10 transition-transform duration-300 group-hover:translate-x-1"
              aria-hidden="true"
            >
              <use href="./shadow-studio-Sprite.svg#arrow-right"></use>
            </svg>
          </button>
        </div>
      </div>
    </div>

    <div
      class="mt-9 md:mt-16 pt-8 border-t border-white/5 flex flex-wrap gap-y-4 gap-x-8 text-gray-500 text-xs font-medium uppercase tracking-wider reveal-item"
      style="--delay: 1100ms"
    >
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 text-aux-secondary" aria-hidden="true">
          <use href="./shadow-studio-Sprite.svg#certificate"></use>
        </svg>
        <span>Verificados por Shadow Studio</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 text-white/40" aria-hidden="true">
          <use href="./shadow-studio-Sprite.svg#people"></use>
        </svg>
        <span>Comunidad de +120 Clientes</span>
      </div>
      <div class="flex-grow text-right hidden md:block">
        <span class="opacity-50">Est. 2025</span>
      </div>
    </div>
  </div>
</header>

<script>
  const observerOptions = {
    root: null,
    rootMargin: "0px",
    threshold: 0.2,
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.remove("opacity-0");

        const items = entry.target.querySelectorAll(".reveal-item");
        items.forEach((item) => {
          item.classList.add("is-visible");
        });

        const line = entry.target.querySelector(".animate-expand-line");
        const text = entry.target.querySelector(".animate-fade-in-right");
        if (line) line.style.width = "2rem"; // w-8
        if (text) text.style.opacity = "1";
        if (text) text.style.transform = "translateX(0)";

        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  const header = document.getElementById("testimonials-header");
  if (header) observer.observe(header);
</script>

<style>
  .outline-text {
    -webkit-text-stroke: 1px rgba(255, 255, 255, 0.1);
    color: transparent;
  }

  @keyframes shine {
    to {
      background-position: 200% center;
    }
  }
  .animate-shine {
    animation: shine 6s linear infinite;
  }

  @keyframes pulseSlow {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.04;
    }
    50% {
      transform: scale(1.05);
      opacity: 0.08;
    }
  }
  .animate-pulse-slow {
    animation: pulseSlow 10s ease-in-out infinite;
  }

  .reveal-item {
    opacity: 0;
    transform: translateY(30px);
    transition:
      opacity 0.8s ease-out,
      transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
    transition-delay: var(--delay, 0ms);
  }

  .reveal-item.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .animate-expand-line {
    transition: width 1s ease-out 0.2s;
  }

  .animate-fade-in-right {
    transform: translateX(-10px);
    transition:
      opacity 0.8s ease-out 0.4s,
      transform 0.8s ease-out 0.4s;
  }
</style>
